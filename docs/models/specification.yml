definitions:
  Specification:
    type: object
    description: equipment specification.
    properties:
      id:
        type: uint
        description: id of the specification being created
        example: 10
      name:
        type: string
        description: equipment specification name.
        example: "teclado"
    required:
      - name
