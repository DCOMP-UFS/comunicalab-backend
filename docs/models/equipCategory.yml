definitions:
  EquipCategory:
    type: object
    properties:
      id:
        type: uint
        description: id of the equipment category being created
        example: 10
      name:
        type: string
        description: equipment category name
        example: gabinete
        max: 100
    required:
      - id
      - name
